// config/dbConfig.js

module.exports = {
    host: 'localhost',         // O host onde o banco de dados MySQL está rodando
    port: 3306,                // Porta padrão do MySQL (3306)
    user: 'seuUsuário',        // Seu nome de usuário do banco de dados
    password: 'suaSenha',      // A senha para acessar o banco de dados
    database: 'seuBancoDeDados' // O nome do banco de dados que você está utilizando
};
